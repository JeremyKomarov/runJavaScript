properties([parameters([booleanParam(description: 'sdfsd', name: 'www')])])
pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'node app.js'
            }
        }
    }
}
